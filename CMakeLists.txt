## CMake Generated by buildtool - NSTreeController_test

## Begin header
cmake_minimum_required(VERSION 3.13)
set(CMAKE_EXPORT_COMPILE_COMMANDS ON)
if (EXISTS CMake/common.cmake)
  include(CMake/common.cmake)
endif()
set(CMAKE_CXX_STANDARD 11)
set(CMAKE_C_COMPILER "clang-9")
set(CMAKE_CXX_COMPILER "clang++-9")
project(NSTreeController_test C CXX)
set(BUILD_FOLDER_NAME "build")
set(CMAKE_SOURCE_DIR ".")

# Begin sources
set (SOURCES
  ${CMAKE_SOURCE_DIR}/NSTreeController_test/main.m
  ${CMAKE_SOURCE_DIR}/NSTreeController_test/AppDelegate.m
  ${CMAKE_SOURCE_DIR}/NSTreeController_test/TreeNode.m
)

if (EXISTS CMake/sources.cmake)
  include(CMake/sources.cmake)
endif()

# Declare target
add_executable(
  NSTreeController_test
  ${SOURCES}
)

# Include directories
target_include_directories(NSTreeController_test PRIVATE
  /usr/GNUstep/Local/Library/Headers
  /usr/GNUstep/System/Library/Headers
)

if (EXISTS CMake/include.cmake)
  include(CMake/include.cmake)
endif()

# Begin resources
set (GLOBAL_RESOURCES
  ${CMAKE_SOURCE_DIR}/NSTreeController_test/Assets.xcassets
  ${CMAKE_SOURCE_DIR}/NSTreeController_test/Base.lproj/MainMenu.xib
  ${CMAKE_SOURCE_DIR}/Info-gnustep.plist
)

if (EXISTS CMake/resources.cmake)
  include(CMake/resources.cmake)
endif()

# Compile options
target_compile_options(NSTreeController_test PRIVATE -MMD -MP -DGNUSTEP -DGNUSTEP_BASE_LIBRARY=1 -DGNU_GUI_LIBRARY=1 -DGNUSTEP_RUNTIME=1 -D_NONFRAGILE_ABI=1 -DGNUSTEP_BASE_LIBRARY=1 -fno-strict-aliasing -fexceptions -fobjc-exceptions -D_NATIVE_OBJC_EXCEPTIONS -pthread -fPIC -Wall -DGSWARN -DGSDIAGNOSE -Wno-import -g -O2 -fobjc-runtime=gnustep-2.0 -fblocks -fconstant-string-class=NSConstantString -I. -I/home/heron/GNUstep/Library/Headers -I/usr/GNUstep/Local/Library/Headers -I/usr/GNUstep/System/Library/Headers)

# Link options
target_link_options(NSTreeController_test PRIVATE -fuse-ld=gold -pthread -fexceptions -rdynamic -fobjc-runtime=gnustep-2.0 -fblocks -L/home/heron/GNUstep/Library/Libraries -L/usr/GNUstep/Local/Library/Libraries -L/usr/GNUstep/System/Library/Libraries -lgnustep-gui -lgnustep-base -lpthread -lobjc -lm)

if (EXISTS CMake/link.cmake)
  include(CMake/link.cmake)
endif()

# Copy resources
set_target_properties(NSTreeController_test PROPERTIES RUNTIME_OUTPUT_DIRECTORY "NSTreeController_test.app")
file(COPY ${GLOBAL_RESOURCES} DESTINATION "NSTreeController_test.app/Resources")
